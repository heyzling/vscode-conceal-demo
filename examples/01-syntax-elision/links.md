# Link targets

See the [Q4 report](https://storage.example.com/exports/2026/q4-final-v3.pdf) for the numbers, and
the [runbook](https://intranet.example.com/ops/runbook?tab=alerts&from=dashboard) if anything is on
fire.

Org-mode calls this a *descriptive link* and ships it as the default. It is also the second of the
two things vim's markdown conceal is most used for.

A generated href nobody reads is the same case: `[the note](kms://record/A3BF9Z)`.
