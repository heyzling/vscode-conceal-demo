# A generated region

Everything between the two fences is rewritten by a daemon; the fences themselves are scaffolding
that never becomes content.

<!-- kms:view query="#todo !#done" limit=20 -->
- [ ] {!A3BF9Z} Buy milk
- [ ] {!K71QMX} Ship the fork
<!-- /kms:view -->

Text after the region continues as normal.
