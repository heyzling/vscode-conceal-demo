# {!A3BF9Z} Project notes

## {!K71QMX} #todo Buy milk

## {!P02LDF} #done Ship the fork

## {!ZQGK9H} #todo Write the demo extension

Every heading above starts with a six-character identity marker written by a sync daemon. Put the
caret at the very start of one of those lines (Home) and type `1. ` — it lands in front of the
marker, because this rule declares `cursorStop: "before"`.
