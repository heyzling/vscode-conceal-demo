# {!A3BF9Z} Project notes, with a badge drawn

## {!K71QMX} #todo Buy milk

## {!P02LDF} #done Ship the fork

The same rule as `notes.md`, with `replaceWith: "°"`. The badge is drawn, so it has a left side and
a right side, each standing for one end of the marker — press the left and right arrows across it
and watch which side the caret takes. `cursorStop` is set on this rule too and has no effect here:
the editor reads it only when nothing is drawn.
